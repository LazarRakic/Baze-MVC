@model IEnumerable<Movie>
@*@model NapredneBP_Project.Models.Movie*@

@{ ViewData["Title"] = "All Movies";
}
<h1>All Movies</h1>

@*<div class="text-center">
            <input type="text" id="movieName" placeholder="Search" />
            <h3>@Html.ActionLink("Search movie by title", "GetMovieByTitle", "Movie", new { title = "Seven" })</h3>
</div>*@

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>Title</th>
            <th>Publishing date</th>
            <th>Description</th>
            <th>Rate</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
        <tr>
            <td width="30%">@obj.Title</td>
            <td width="30%">@obj.PublishingDate</td>
            <td width="50%">@obj.Description</td>
            <td width="50%">@obj.Rate</td>
            <td width="100%">
                <div class="w-75 btn-group" role="group">
                    <a asp-controller="Movie" asp-action="GetMovieById" asp-route-id="@obj.Id"
                       class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i>About</a>
                    <a asp-controller="Movie" asp-action="DeleteMovieById" asp-route-id="@obj.Id"
                       class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i>Delete</a>
                </div>
            </td>
        </tr>
        }
    </tbody>
</table>
